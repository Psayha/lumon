server {
    server_name n8n.psayha.ru;

    root /var/www/lumon/back/nginx-simple-pages;
    index n8n.html;

    location / {
        try_files $uri $uri/ =404;
    }

    listen 80;
    listen [::]:80;
}

