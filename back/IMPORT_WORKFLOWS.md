# 📥 Импорт workflows на сервере после деплоя

После каждого деплоя через Git, workflows обновляются на сервере.

## Автоматический импорт (если настроен)

Если скрипт `import-workflows.sh` настроен в GitHub Actions, workflows импортируются автоматически.

## Ручной импорт

Если нужно импортировать workflows вручную на сервере:

```bash
cd /var/www/back

# Убедись что workflows файлы на месте
ls -la n8n/workflows/

# Используй скрипт импорта (если есть)
./import-workflows.sh

# Или импортируй через n8n API вручную
# См. инструкции в CONNECT_FRONTEND.md
```

## Проверка

1. Зайди в n8n: `http://91.229.10.47:5678` (или `https://n8n.psayha.ru` после SSL)
2. Проверь что все 5 workflows видны:
   - Create User
   - Create Chat
   - Save Message
   - Get Chat History
   - Analytics
3. Активируй их все (переключатель "Active")
4. Настрой PostgreSQL credentials (см. CONNECT_FRONTEND.md)

