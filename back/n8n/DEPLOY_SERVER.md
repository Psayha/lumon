# 🚀 Деплой n8n Workflows на сервер

## 📋 ШАГ 1: Подготовка сервера

### Что нужно на сервере:

1. **Docker** установлен (проверьте: `docker --version`)
2. **Docker Compose** установлен (проверьте: `docker-compose --version`)
3. **Git** установлен (проверьте: `git --version`)

### Если Docker НЕ установлен - установите сейчас:

**Выполните эти команды на сервере по порядку:**

```bash
# 1. Обновить пакеты
apt-get update

# 2. Установить необходимые пакеты
apt-get install -y ca-certificates curl gnupg lsb-release

# 3. Добавить официальный GPG ключ Docker
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
chmod a+r /etc/apt/keyrings/docker.gpg

# 4. Добавить репозиторий Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# 5. Обновить список пакетов
apt-get update

# 6. Установить Docker
apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 7. Проверить установку
docker --version
docker compose version
```

**Если вы используете Debian вместо Ubuntu, используйте эту команду для шага 4:**

```bash
# Для Debian (заменить шаг 4)
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
  $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
```

**Если у вас другая система (CentOS/RHEL):**

```bash
# Для CentOS/RHEL
yum install -y yum-utils
yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
yum install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
systemctl start docker
systemctl enable docker
```

**После установки проверьте:**

```bash
docker --version
docker compose version
```

Должны увидеть версии Docker и Docker Compose.

---

## 📋 ШАГ 2: Клонирование кода на сервер

### 2.1 Подключитесь к серверу по SSH

```bash
ssh user@ваш-сервер.com
```

### 2.2 Перейдите в нужную папку (например, /home/user)

```bash
cd ~
```

### 2.3 Клонируйте репозиторий (если еще не клонирован)

```bash
git clone https://github.com/Psayha/lumon.git
```

Или если уже есть:

```bash
cd lumon
git pull origin main
```

### 2.4 Перейдите в папку back

```bash
cd lumon/back
```

---

## 📋 ШАГ 3: Создание .env файла

### 3.1 Скопируйте пример файла

```bash
cp .env.example .env
```

### 3.2 Откройте файл для редактирования

```bash
nano .env
```

Или через vim:

```bash
vim .env
```

### 3.3 Заполните файл .env

Замените значения на ваши:

```
# PostgreSQL / Supabase
POSTGRES_USER=postgres
POSTGRES_PASSWORD=moy_nadezhny_parol_123
POSTGRES_DB=lumon

# n8n
N8N_USER=admin
N8N_PASSWORD=moy_parol_n8n_456
N8N_ENCRYPTION_KEY=moy_sluchayny_klyuch_32_simvola_12345678

# n8n Host (если есть домен)
# N8N_HOST=ваш-домен.com
# N8N_PROTOCOL=https
```

**Что нужно изменить:**
- `POSTGRES_PASSWORD` - придумайте надежный пароль для базы данных
- `N8N_PASSWORD` - придумайте пароль для входа в n8n
- `N8N_ENCRYPTION_KEY` - придумайте случайную строку из 32 символов

### 3.4 Сохраните файл

В nano: `Ctrl+O` (сохранить), затем `Enter`, затем `Ctrl+X` (выйти)

В vim: `Esc`, затем `:wq` (сохранить и выйти)

---

## 📋 ШАГ 4: Запуск Docker контейнеров

### 4.1 Запустите все сервисы

```bash
docker-compose up -d
```

**Что происходит:** Запускаются 3 контейнера:
- PostgreSQL база данных
- Supabase Studio (веб-интерфейс для БД)
- n8n (workflows)

### 4.2 Проверьте, что всё запустилось

```bash
docker-compose ps
```

**Должно показать 3 контейнера со статусом "Up":**
- `lumon-supabase-db` 
- `lumon-supabase-studio`
- `lumon-n8n`

Если какой-то не запустился - смотрите ошибки:

```bash
docker-compose logs n8n
```

---

## 📋 ШАГ 5: Открытие n8n в браузере

### 5.1 Узнайте IP адрес сервера

```bash
curl ifconfig.me
```

Или используйте домен, если он настроен.

### 5.2 Откройте n8n в браузере

Перейдите по адресу: `http://ВАШ_IP_АДРЕС:5678`

Например: `http://192.168.1.100:5678`

### 5.3 Войдите в n8n

- **Логин:** `admin` (или тот, что указали в .env)
- **Пароль:** тот пароль, что указали в `.env` для `N8N_PASSWORD`

## 📥 ШАГ 6: Создание PostgreSQL Credential в n8n

### 6.1 Откройте настройки credentials

В n8n:
1. Нажмите на иконку **Settings** (шестеренка) слева внизу
2. Выберите **Credentials**
3. Нажмите **New Credential** (кнопка справа вверху)
4. Найдите и выберите **PostgreSQL**

### 6.2 Заполните данные подключения

В форме заполните:

- **Name:** `Lumon PostgreSQL`
- **Host:** `supabase-db` ← **ВНИМАНИЕ:** это имя контейнера из docker-compose, не IP!
- **Port:** `5432`
- **Database:** `lumon`
- **User:** `postgres`
- **Password:** тот же пароль, что в `.env` для `POSTGRES_PASSWORD`
- **SSL:** оставьте **выключенным** (переключатель влево)

### 6.3 Сохраните credential

Нажмите **Save** (кнопка справа внизу)

---

## 📥 ШАГ 7: Импорт workflows в n8n

### 7.1 Откройте раздел Workflows

В n8n:
1. Нажмите **Workflows** в левом меню
2. Нажмите **Import from File** (кнопка справа вверху)

### 7.2 Импортируйте первый workflow

1. Нажмите **Choose File** или перетащите файл
2. Перейдите на сервере в папку: `/home/user/lumon/back/n8n/workflows/`
3. Выберите файл: `save-message.json`
4. Нажмите **Import**

### 7.3 Импортируйте остальные 4 workflow

Повторите для каждого файла:
- `get-chat-history.json`
- `create-user.json`
- `create-chat.json`
- `analytics.json`

**Итого должно быть 5 workflows в списке.**

---

## 📥 ШАГ 8: Настройка Credentials в workflows

### 8.1 Откройте первый workflow

Нажмите на **Save Message** workflow

### 8.2 Найдите PostgreSQL node

В workflow найдите узел с названием **Insert Message** (зеленый, PostgreSQL)

### 8.3 Откройте node и выберите credential

1. Кликните на узел **Insert Message**
2. В правой панели найдите поле **Credential for PostgreSQL**
3. Выберите из списка: **Lumon PostgreSQL**
4. Нажмите **Save** (если нужно)

### 8.4 Сохраните workflow

Нажмите кнопку **Save** в правом верхнем углу

### 8.5 Повторите для всех workflows

Для каждого workflow откройте все PostgreSQL nodes и выберите **Lumon PostgreSQL**:

**Create User workflow:**
- Check User → Lumon PostgreSQL
- Update User → Lumon PostgreSQL  
- Insert User → Lumon PostgreSQL
- Get User → Lumon PostgreSQL

**Create Chat workflow:**
- Insert Chat → Lumon PostgreSQL
- Get Chat Data → Lumon PostgreSQL

**Save Message workflow:**
- Insert Message → Lumon PostgreSQL

**Get Chat History workflow:**
- Select Messages → Lumon PostgreSQL

**Analytics workflow:**
- Insert Event → Lumon PostgreSQL

---

## 📥 ШАГ 9: Активация workflows

### 9.1 Активируйте первый workflow

1. Откройте любой workflow
2. В правом верхнем углу найдите переключатель **Active**
3. Переключите его в положение **ON** (должен стать синим)

### 9.2 Активируйте все остальные workflows

Повторите для каждого из 5 workflows:
- Save Message → Active = ON
- Get Chat History → Active = ON
- Create User → Active = ON
- Create Chat → Active = ON
- Analytics → Active = ON

**Все workflows должны быть активными (переключатель синий).**

## ✅ ШАГ 10: Проверка работы

### 10.1 Проверьте через браузер

Откройте в браузере адрес webhook:

```
http://ВАШ_IP:5678/webhook/create-user
```

Должна появиться страница с сообщением об ошибке (это нормально, просто endpoint работает).

### 10.2 Проверьте через curl (на сервере)

```bash
# Тест Create User
curl -X POST http://localhost:5678/webhook/create-user \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789, "first_name": "Test User"}'
```

**Ожидаемый результат:** JSON ответ вида `{"success": true, "data": {...}}`

### 10.3 Проверьте Executions в n8n

1. В n8n нажмите **Executions** в левом меню
2. Вы должны увидеть выполнение workflow "Create User"
3. Кликните на execution, чтобы увидеть детали
4. Если все зеленое - работает! ✅

### 10.4 Если ошибка

- Откройте execution в n8n
- Посмотрите, на каком node ошибка
- Проверьте, что credential выбран правильно
- Проверьте логи: `docker-compose logs n8n`

## 🔒 ШАГ 11: Безопасность (важно для продакшена!)

### 11.1 Поменяйте пароли

Если вы использовали простые пароли - поменяйте их в `.env`:
1. Откройте `.env` файл
2. Придумайте сложные пароли
3. Сохраните файл
4. Перезапустите: `docker-compose restart n8n supabase-db`

### 11.2 Настройте Firewall

Закройте ненужные порты:

```bash
# Ubuntu/Debian
sudo ufw allow 5678/tcp  # только n8n
sudo ufw enable
```

### 11.3 Настройте HTTPS (опционально)

Если у вас есть домен, настройте nginx как reverse proxy для n8n.

### 11.4 Настройте бэкапы

Создайте скрипт для автоматических бэкапов:

```bash
# Создайте файл backup.sh
cat > /home/user/backup-lumon.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
cd /home/user/lumon/back
docker-compose exec -T supabase-db pg_dump -U postgres lumon > backup_$DATE.sql
# Удалить бэкапы старше 7 дней
find . -name "backup_*.sql" -mtime +7 -delete
EOF

chmod +x /home/user/backup-lumon.sh
```

Добавьте в cron для автоматических бэкапов каждый день:

```bash
crontab -e
# Добавьте строку:
0 2 * * * /home/user/backup-lumon.sh
```

## 🔄 ШАГ 12: Как обновить workflows в будущем

Когда нужно будет обновить workflows:

### 12.1 На локальной машине (где редактируете)

1. Откройте workflow в n8n локально
2. Внесите изменения
3. Экспортируйте: Workflow → Download (кнопка справа вверху)
4. Сохраните файл в `back/n8n/workflows/`
5. Закоммитьте: `git add . && git commit -m "update workflow" && git push`

### 12.2 На сервере

1. Обновите код: `cd /home/user/lumon && git pull`
2. В n8n: Workflows → Import from File → выберите обновленный файл
3. Выберите "Import as new" или замените существующий
4. Проверьте credentials (выберите "Lumon PostgreSQL" в PostgreSQL nodes)
5. Активируйте workflow (Active = ON)

## 📊 Полезные команды для мониторинга

### Посмотреть логи:

```bash
cd /home/user/lumon/back

# Все сервисы сразу
docker-compose logs -f

# Только n8n
docker-compose logs -f n8n

# Только база данных
docker-compose logs -f supabase-db
```

Чтобы выйти из логов: `Ctrl+C`

### Проверить статус контейнеров:

```bash
docker-compose ps
```

### Проверить, работает ли база данных:

```bash
docker-compose exec supabase-db pg_isready -U postgres
```

Должно показать: `postgres (localhost:5432): accepting connections`

### Перезапустить сервис:

```bash
# Перезапустить n8n
docker-compose restart n8n

# Перезапустить всё
docker-compose restart
```

### Остановить всё:

```bash
docker-compose down
```

### Запустить заново:

```bash
docker-compose up -d
```

## 🐛 Решение проблем

### Проблема: n8n не запускается

**Решение:**
```bash
# 1. Посмотрите логи
cd /home/user/lumon/back
docker-compose logs n8n

# 2. Проверьте .env файл (правильные ли пароли)
cat .env

# 3. Проверьте, занят ли порт
netstat -tuln | grep 5678
```

### Проблема: PostgreSQL не доступен

**Решение:**
```bash
# 1. Посмотрите логи базы данных
docker-compose logs supabase-db

# 2. Проверьте, запущен ли контейнер
docker-compose ps

# 3. Попробуйте подключиться вручную
docker-compose exec supabase-db psql -U postgres -d lumon
```

### Проблема: Workflows не выполняются

**Решение:**
1. Проверьте в n8n, что workflows активированы (Active = ON, синий переключатель)
2. Откройте workflow и проверьте каждый PostgreSQL node:
   - Кликните на node
   - Убедитесь, что выбран credential "Lumon PostgreSQL"
   - Если нет - выберите и сохраните
3. Посмотрите Executions в n8n - там будут видны ошибки
4. Проверьте логи: `docker-compose logs n8n`

### Проблема: "Cannot connect to database"

**Решение:**
1. Проверьте credential в n8n:
   - Host должен быть `supabase-db` (не localhost!)
   - Password должен совпадать с `POSTGRES_PASSWORD` из `.env`
2. Проверьте, запущена ли база:
   ```bash
   docker-compose ps supabase-db
   ```
   Должен быть статус "Up"

## 📝 Checklist деплоя

- [ ] Docker и Docker Compose установлены
- [ ] `.env` файл создан с продакшн значениями
- [ ] `docker-compose up -d` выполнен успешно
- [ ] Все контейнеры запущены (`docker-compose ps`)
- [ ] n8n доступен через браузер
- [ ] PostgreSQL Credential создан в n8n
- [ ] Все 5 workflows импортированы
- [ ] Credentials настроены в каждом workflow
- [ ] Все workflows активированы
- [ ] Тестовые запросы работают
- [ ] HTTPS настроен (для продакшена)
- [ ] Firewall настроен
- [ ] Бэкапы настроены

## ✅ Готово!

После выполнения всех шагов, ваш сервер готов к работе!

Frontend может отправлять запросы на:
- `http://ваш-сервер:5678/webhook/create-user`
- `http://ваш-сервер:5678/webhook/create-chat`
- `http://ваш-сервер:5678/webhook/save-message`
- `http://ваш-сервер:5678/webhook/get-chat-history`
- `http://ваш-сервер:5678/webhook/analytics`

