# 📝 Список измененных workflows для обновления

## 🔄 ТЕСТИРУЕТСЯ СЕЙЧАС

### 1. ✅ **chat.create.json** 
**Изменения:**
- ✅ Добавлено логирование `chat.created` в `audit_events`
- ✅ Исправлены URL авторизации на `https://n8n.psayha.ru`
- ✅ Добавлен узел "Prepare Audit Data" для подготовки данных
- ✅ Добавлен узел "Check Audit Log" для отладки
- ✅ Упрощен SQL запрос для логирования

**Статус:** ТЕСТИРУЕТСЯ - нужно проверить, появляются ли логи в админ-панели

---

## ⏳ ОЖИДАЮТ ОБНОВЛЕНИЯ (после успешного тестирования chat.create)

### Приоритет 1: Основные действия пользователей
2. ⏳ **save-message.json** 
   - Логирование `message.sent`
   - Исправлен URL на `https://n8n.psayha.ru`

3. ⏳ **auth.init.v3.json** 
   - Логирование `auth.login`
   - Headers передаются через промежуточный узел

4. ⏳ **auth.logout.json** 
   - Логирование `auth.logout`
   - Headers передаются через промежуточный узел

### Приоритет 2: Административные действия
5. ⏳ **admin.login.json** 
   - Логирование `admin.login`
   - Headers передаются через промежуточный узел

6. ⏳ **admin.user-limits-update.json** 
   - Логирование `admin.user_limits_updated`
   - Headers передаются через промежуточный узел

7. ⏳ **admin.ab-experiment-create.json** 
   - Логирование `admin.ab_experiment_created`
   - Headers передаются через промежуточный узел

8. ⏳ **admin.ab-experiment-update.json** 
   - Логирование `admin.ab_experiment_updated`
   - Headers передаются через промежуточный узел

### Исправления URL авторизации (не критично, но желательно)
9. ⏳ **chat.list.json** 
   - Исправлен URL на `https://n8n.psayha.ru`

10. ⏳ **get-chat-history.json** 
    - Исправлен URL на `https://n8n.psayha.ru`

11. ⏳ **analytics.json** 
    - Исправлен URL на `https://n8n.psayha.ru`

12. ⏳ **auth.set-viewer-role.json** 
    - Исправлен URL на `https://n8n.psayha.ru`

---

## 📋 Инструкция по обновлению

1. **Сначала:** Обновить `chat.create.json` и протестировать
2. **Проверить:** Логи появляются в админ-панели → таб "System Logs"
3. **Если работает:** Обновить остальные workflows из списка выше
4. **Если не работает:** Проверить execution logs в n8n для узла "Log Audit Event"

---

**Последнее обновление:** 9 ноября 2025

