# Production Environment Configuration для СЕРВЕРА
# Скопируйте этот файл в .env на сервере:
# cp .env.server.example .env

# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (Local PostgreSQL in Docker на сервере)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=lumon_dev_password
DB_DATABASE=lumon
DB_SSL=false

# OpenAI Configuration (опционально)
OPENAI_API_KEY=sk-proj-optional-add-later-if-needed

# Session Configuration
SESSION_EXPIRY_DAYS=7

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Telegram Bot (замените на ваш токен)
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# Admin credentials (ИЗМЕНИТЕ ЭТО!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_THIS_PASSWORD_MINIMUM_12_CHARS
