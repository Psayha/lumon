# 🚀 План следующих шагов (чистый список)

> **Дата:** 6 ноября 2025

---

## 1) Инфраструктура и стабильность
- ✅ Убрать дубли `server_name` в nginx (оставить по одному сервер-блоку на домен).
- ✅ Проверить Supabase Studio: устойчивая работа с `DATABASE_URL`/`PG*`; устранить `ECONNRESET`.
- ✅ Финализировать окружение n8n: `WEBHOOK_URL`, `N8N_EDITOR_BASE_URL`; перезапуск n8n.
- ✅ Nginx конфигурация: все домены работают с SSL, правильная маршрутизация.
- ✅ Certbot renew: systemd таймер активен, все сертификаты действительны 81-89 дней.

## 2) Backend (n8n)
- ✅ Идемпотентность: реализована в `chat.save-message` (проверка/кэширование через `idempotency_keys`).
- ✅ Rate limiting: полностью интегрировано.
  - Subworkflow `rate-limit.check` создан и активен
  - Интегрировано в `chat.create`, `chat.save-message`, `analytics`
  - Лимиты: 30 req/min (mutating), 100 req/min (analytics)
  - Инструкция: `back/SETUP_RATE_LIMITING.md`
- ✅ Analytics: workflow `analytics.log-event` готов (логирование в `audit_events` через `auth.validate`).

## 3) Frontend
- ✅ Прод-конфигурация использует `VITE_API_URL=https://n8n.psayha.ru` (уже настроено).
- ✅ ApiTestPage: полностью готов.
  - Автоматическая проверка всех эндпоинтов (кнопка "Тестировать все")
  - Валидация обязательных полей (chat_id для chat-save-message и chat-get-history)
  - Автоматическая подстановка chat_id из созданного чата
  - Понятные сообщения об ошибках с инструкциями
  - Правильные тестовые данные для всех эндпоинтов (включая analytics-log-event)
  - Показывает сводку результатов с цветовой индикацией
- ✅ Улучшены сообщения об ошибках сети/auth: валидация перед запросом, понятные сообщения с troubleshooting.

## 4) Admin
- ✅ Реальная аутентификация: добавить `/api/admin/login` в n8n, хранение токена, отказ от заглушки.
  - Создана миграция `20251106000000_admin_users.sql` (таблицы `admin_users`, `admin_sessions`)
  - Создан workflow `admin.login.json` для `/webhook/admin-login`
  - Обновлен `AdminLogin.tsx` для использования реального API
  - Инструкция: `back/SETUP_ADMIN_LOGIN.md`
- RBAC в админке: ограничить доступ к вкладкам по ролям.

## 5) CI/CD
- Добавить деплой шага для артефакта `dist-admin` (rsync на `/var/www/lumon2/dist-admin`).
- Мини-пайплайн smoke-тестов (curl поддоменов) после деплоя.

## 6) Обслуживание
- Бэкапы БД (ежедневно) и экспорт n8n workflows (еженедельно).
- Health-checks/алёрты: `/health` в nginx, мониторинг контейнеров.

---

### Критерии готовности
- ✅ Нет предупреждений `conflicting server name`.
- ✅ `sb.psayha.ru` стабильно грузит схемы; логи Studio без `ECONNRESET`.
- ✅ Все запросы идут через `https://n8n.psayha.ru`; ApiTestPage полностью готов с валидацией и улучшенными сообщениями об ошибках.
- ✅ Все домены работают: n8n.psayha.ru (200), sb.psayha.ru (307), admin.psayha.ru (200), psayha.ru (200) с SSL.
- ✅ `chat.save-message` устойчив к ретраям через `idempotency_keys`.
- ✅ Rate limiting активно: workflows возвращают 429 при превышении лимитов.
- ✅ Вход в админку через реальный бэкенд (`/webhook/admin-login`); роли ограничивают вкладки (в процессе).
- Workflow деплоя админки собирает и выкатывает `dist-admin` на сервер.
- ✅ Certbot renew проходит `--dry-run` (таймер активен, сертификаты действительны 81-89 дней).

---

## 🎯 Быстрый старт (MVP)

Если нужно быстро улучшить продукт, начни с:

1. **RBAC UI ограничения** (1-2 дня)
   - Блокировка действий для viewer
   - Badge "Только просмотр"

2. **Список чатов** (3-4 дня)
   - Базовая страница со списком
   - Навигация между чатами

3. **Идемпотентность** (1-2 дня)
   - Защита от дублирования запросов

**Итого:** 5-8 дней для MVP улучшений

---

**Готов начать с любого приоритета!** 🚀

