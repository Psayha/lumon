# Админ-панель Lumon Platform

## Структура

```
adminpage/
├── AdminPage.tsx          # Главный компонент
├── components/
│   └── AdminLogin.tsx    # Компонент авторизации
├── tabs/
│   ├── CompaniesTab.tsx   # Таб с компаниями
│   ├── LegalDocsTab.tsx  # Таб с юр документами
│   └── AIDocumentsTab.tsx # Таб с документами для ИИ
├── main.tsx              # Entry point
├── index.html            # HTML шаблон
├── vite.config.ts        # Конфигурация Vite (ESM compatible)
├── postcss.config.js     # Isolated PostCSS config
└── tsconfig.json         # TypeScript config with node types
```

## 🛠️ Сборка и Конфигурация

**Важно:** Admin Panel имеет изолированную конфигурацию сборки, чтобы избежать конфликтов с основным проектом.

- `postcss.config.js`: Отдельный конфиг для Tailwind, предотвращает ошибки с `fraction.js`.
- `vite.config.ts`: Использует ESM-совместимые пути (`fileURLToPath`).
- `tsconfig.json`: Включает типы `node` для корректной работы `path` и `url` модулей.

## Функционал

### 1. Авторизация

- Простая форма входа (admin / admin для разработки)
- Сохранение токена в localStorage

### 2. Таб "Компании"

- Список всех компаний
- Поиск по названию
- Раскрывающийся список пользователей компании
- Отображение ролей пользователей

### 3. Таб "Юр документы"

- Список юридических документов
- Редактирование документов
- Обновление текста для модалки AgreementModal

### 4. Таб "Документы для ИИ"

- Загрузка документов (PDF, DOC, DOCX, TXT)
- Список загруженных документов
- Удаление документов
- Статусы обработки

## TODO

- [ ] Интегрировать с реальным API
- [ ] Добавить валидацию форм
- [ ] Добавить обработку ошибок
- [ ] Добавить уведомления об успехе/ошибках
- [ ] Добавить пагинацию для списков
- [ ] Добавить фильтры и сортировку
