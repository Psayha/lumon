# 📊 Расширенная аналитика и админ-функции Lumon Platform

> **Обновлено:** 9 ноября 2025

## Обзор

Система расширенной аналитики и функций для админов включает:
- Просмотр логов системы
- Управление пользователями и их лимитами
- Статистика использования платформы
- A/B тестирование функций

---

## 📋 Структура

### Таблицы базы данных

**Логи и аудит:**
- `audit_events` - события аудита (действия пользователей, IP, user agent)

**Управление пользователями:**
- `user_limits` - лимиты пользователей (generations_per_day, chats_per_month, etc.)
  - `user_id` - ID пользователя
  - `limit_type` - тип лимита
  - `limit_value` - значение лимита
  - `current_usage` - текущее использование
  - `reset_at` - время сброса

**A/B тестирование:**
- `ab_experiments` - эксперименты (название, описание, процент трафика)
- `ab_assignments` - назначения пользователей на варианты (A/B)
- `ab_events` - события экспериментов (конверсии, клики)

**Статистика платформы:**
- `platform_stats` - ежедневная агрегированная статистика
  - `date` - дата
  - `total_users`, `active_users` - пользователи
  - `total_companies`, `total_chats`, `total_messages` - активность
  - `new_users`, `new_companies` - новые за период
  - `metrics` (JSONB) - дополнительные метрики

### API Endpoints

**Логи:**
- `GET /webhook/admin-logs-list` - список логов с фильтрацией
  - Query params: `limit`, `offset`, `action`, `user_id`

**Пользователи:**
- `GET /webhook/admin-users-list` - список пользователей
  - Query params: `limit`, `offset`, `search`

**Лимиты:**
- `GET /webhook/admin-user-limits-list` - список лимитов
  - Query params: `user_id` (опционально)
- `POST /webhook/admin-user-limits-update` - обновление лимита
  - Body: `user_id`, `limit_type`, `limit_value`, `reset_at`

**Статистика:**
- `GET /webhook/admin-stats-platform` - статистика платформы

**A/B тестирование:**
- `GET /webhook/admin-ab-experiments-list` - список экспериментов
- `POST /webhook/admin-ab-experiment-create` - создание эксперимента
  - Body: `name`, `description`, `feature_name`, `enabled`, `traffic_percentage`, `variant_a_config`, `variant_b_config`
- `POST /webhook/admin-ab-experiment-update` - обновление эксперимента
  - Body: `id`, `enabled`, `traffic_percentage`, `variant_a_config`, `variant_b_config`

### Workflows

**Admin API workflows:**
- `admin.logs-list.json` - просмотр логов
- `admin.users-list.json` - список пользователей (✅ исправлен: credentials, поиск по telegram_id, обработка ошибок)
- `admin.stats-platform.json` - статистика платформы (✅ исправлен: credentials, обработка ошибок)
- `admin.user-limits-list.json` - список лимитов (✅ исправлен: credentials, обработка ошибок, сбор всех строк)
- `admin.user-limits-update.json` - обновление лимита
- `admin.ab-experiments-list.json` - список экспериментов (✅ улучшена обработка ошибок)
- `admin.ab-experiment-create.json` - создание эксперимента
- `admin.ab-experiment-update.json` - обновление эксперимента
- `admin.validate.json` - валидация admin токена (✅ активирован)

**Cron workflows:**
- `cron.aggregate-stats.json` - агрегация статистики (каждый час)

### Табы админки

**LogsTab.tsx:**
- Просмотр логов системы
- Фильтрация по действиям
- Поиск по логам
- Экспорт в CSV
- Пагинация

**UsersTab.tsx:**
- Список пользователей
- Поиск пользователей
- Просмотр информации (чаты, компании)
- Управление лимитами пользователей

**AnalyticsTab.tsx:**
- Карточки с метриками платформы
- Статистика пользователей, компаний, чатов, сообщений
- Активные сессии

**ABTestingTab.tsx:**
- Список экспериментов
- Создание новых экспериментов
- Включение/отключение экспериментов
- Просмотр статистики по вариантам

---

## 🔧 Использование

### Просмотр логов

```bash
curl -X GET "https://n8n.psayha.ru/webhook/admin-logs-list?limit=50&offset=0&action=chat.create" \
  -H "Authorization: Bearer <admin_token>"
```

### Управление лимитами пользователя

```bash
curl -X POST "https://n8n.psayha.ru/webhook/admin-user-limits-update" \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "uuid",
    "limit_type": "generations_per_day",
    "limit_value": 10
  }'
```

### Создание A/B эксперимента

```bash
curl -X POST "https://n8n.psayha.ru/webhook/admin-ab-experiment-create" \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "New UI Design",
    "feature_name": "ui_design",
    "enabled": true,
    "traffic_percentage": 50,
    "variant_a_config": {"theme": "light"},
    "variant_b_config": {"theme": "dark"}
  }'
```

---

## 📊 Автоматическая агрегация статистики

Cron workflow `cron.aggregate-stats` выполняется каждый час и:
- Собирает статистику по пользователям, компаниям, чатам, сообщениям
- Сохраняет данные в таблицу `platform_stats`
- Обновляет существующие записи за текущий день

---

## 🎯 Типы лимитов

Поддерживаемые типы лимитов:
- `generations_per_day` - генерации в день
- `chats_per_month` - чаты в месяц
- `messages_per_day` - сообщения в день
- Можно добавить свои типы лимитов

---

## 🔬 A/B тестирование

### Структура эксперимента

1. **Создание эксперимента** - настройка названия, функции, процента трафика
2. **Назначение пользователей** - автоматическое распределение на варианты A/B
3. **Отслеживание событий** - запись событий (view, click, conversion)
4. **Анализ результатов** - сравнение метрик между вариантами

### Использование в коде

```typescript
// Получение варианта для пользователя
const variant = await getABVariant(userId, 'ui_design');
if (variant === 'A') {
  // Вариант A
} else {
  // Вариант B
}

// Отслеживание события
await trackABEvent(experimentId, userId, variant, 'click');
```

---

## 🔧 Исправления и улучшения (9 ноября 2025)

### Исправленные проблемы

1. **Обработка ошибок PostgreSQL:**
   - Все workflows теперь правильно обрабатывают ошибки базы данных
   - Детальные сообщения с кодами ошибок (code, message, detail, hint, where)
   - Обработка формата NodeOperationError от n8n
   - Правильное извлечение сообщений из вложенных объектов

2. **Исправление credentials:**
   - Все workflows используют правильное подключение к базе `lumon` (ID: `OPy15M2cOLEss5yi`)
   - Исправлено подключение в `admin.users-list`, `admin.stats-platform`, `admin.user-limits-list`

3. **Сбор данных из PostgreSQL:**
   - Исправлена обработка множественных items от PostgreSQL node
   - Все строки результатов теперь правильно собираются в массивы
   - Исправлено в `admin.users-list` и `admin.user-limits-list`

4. **Активация workflows:**
   - `admin.validate.json` активирован (active: true)
   - Все admin workflows должны быть активны для работы webhooks

5. **Создание таблиц:**
   - ✅ `user_limits` - создана с индексами и триггерами
   - ✅ `ab_experiments` - создана с индексами и триггерами
   - ✅ `ab_assignments` - создана с индексами

### SQL скрипты для миграций

- `back/n8n/workflows/create-user-limits-table.sql` - создание таблицы user_limits
- `back/n8n/workflows/create-ab-tables.sql` - создание таблиц A/B тестирования
- `back/n8n/workflows/create-ab-tables-safe.sql` - безопасное создание таблиц A/B тестирования

**Последнее обновление:** 9 ноября 2025

