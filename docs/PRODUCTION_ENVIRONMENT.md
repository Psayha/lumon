# 📋 Production Environment - Полная Карта Сервера

> **Дата последнего обновления:** 18 ноября 2025
> **Сервер:** cv5403621.novalocal (192.168.0.218)

---

## 🖥️ Системная Информация

| Параметр | Значение |
|----------|----------|
| **ОС** | Ubuntu 24.04.3 LTS |
| **Kernel** | Linux 6.8.0-85-generic x86_64 |
| **Hostname** | cv5403621.novalocal |
| **IP** | 192.168.0.218 |
| **Node.js** | v20.19.5 |
| **npm** | 10.8.2 |

---

## 📁 Структура Проекта

### Основная Директория: `/home/user/lumon/`

```
/home/user/lumon/
├── dist/                      # ✅ Собран 17.11.2025 20:23
│   └── index.html             # Основной фронтенд (1171 байт)
├── dist-admin/                # ✅ Собран 17.11.2025 20:23
│   └── index.html             # Админка (1346 байт)
├── back/api/
│   ├── dist/                  # ✅ Собран 17.11.2025 23:48
│   │   └── main.js            # NestJS API
│   ├── node_modules/          # ✅ 526 пакетов установлено
│   └── .env                   # ✅ Конфигурация БД
├── node_modules/              # ✅ 136 пакетов установлено
├── adminpage/                 # ⚠️ НЕТ node_modules/
│   ├── src/
│   └── package.json           # ❌ НЕ НАЙДЕН
├── src/                       # Исходники фронтенда
├── front/                     # Дополнительные страницы
└── .git/                      # Git репозиторий
```

### Web-сервер: `/var/www/`

```
/var/www/
├── lumon2/                    # ⚠️ Обновлен 18.11.2025 21:54
│   ├── index.html             # (1411 байт) - ДРУГАЯ СБОРКА!
│   ├── assets/
│   └── dist-admin/            # Админка
│       └── index.html
├── adminpage/                 # Старая админка (не используется)
└── back/                      # Старый бэкенд (не используется)
```

**⚠️ ВАЖНО:** Файлы в `/var/www/lumon2/` были обновлены **18 ноября 21:54**, но это **НЕ** из git репозитория `/home/user/lumon/dist/` (который собран 17 ноября 20:23). Это означает кто-то вручную скопировал другую сборку!

---

## 🌐 Nginx Конфигурация

### Веб-сайты

| Домен | Путь | Назначение |
|-------|------|------------|
| **psayha.ru** | `/var/www/lumon2/` | Основной фронтенд |
| **admin.psayha.ru** | `/var/www/lumon2/dist-admin/` | Админ-панель |
| **n8n.psayha.ru** | `proxy → localhost:3000` | NestJS API |
| **sb.psayha.ru** | `proxy → localhost:3001` | Supabase Studio |

### Порты

| Порт | Сервис | Статус |
|------|--------|--------|
| **80** | Nginx HTTP | ✅ Работает |
| **443** | Nginx HTTPS | ✅ Работает |
| **3000** | NestJS API | ✅ Работает |
| **3001** | Supabase Studio (Docker) | ✅ Работает |
| **5432** | PostgreSQL (Docker) | ✅ Работает |
| **5678** | n8n (Docker) | ⚠️ Запущен, но не используется |

---

## 🐳 Docker Контейнеры

| Контейнер | Image | Порты | Статус |
|-----------|-------|-------|--------|
| **lumon-supabase-db** | postgres:15-alpine | 0.0.0.0:5432→5432 | ✅ Healthy (36h) |
| **lumon-supabase-studio** | supabase/studio:latest | 127.0.0.1:3001→3000 | ✅ Running (36h) |
| **lumon-n8n** | n8nio/n8n:latest | 127.0.0.1:5678→5678 | ⚠️ Running (не используется) |

---

## 🗄️ База Данных PostgreSQL

**Подключение:**
- **Host:** localhost (через Docker)
- **Port:** 5432
- **Database:** lumon
- **User:** postgres
- **SSL:** false

**Таблицы:** 60 таблиц (включая):
- `users` - пользователи
- `sessions` - сессии авторизации
- `chats` - чаты
- `messages` - сообщения (✅ с колонкой `metadata`)
- `companies` - компании
- `analytics_events` - события аналитики
- `audit_events` - аудит
- `admin_users` - админы
- и другие...

---

## 🚀 Systemd Сервисы

### lumon-api.service (NestJS API)

**Конфигурация:** `/etc/systemd/system/lumon-api.service`

```ini
[Unit]
Description=Lumon NestJS API Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/home/user/lumon/back/api
Environment=NODE_ENV=production
Environment=PORT=3000
ExecStart=/usr/bin/node dist/main.js
Restart=on-failure
StandardOutput=append:/var/log/lumon-api.log
StandardError=append:/var/log/lumon-api-error.log

[Install]
WantedBy=multi-user.target
```

**Статус:** ✅ Active (running) с 18.11.2025 22:05:55
**Логи:**
- `/var/log/lumon-api.log` - обычные логи
- `/var/log/lumon-api-error.log` - ошибки

**Управление:**
```bash
sudo systemctl status lumon-api
sudo systemctl restart lumon-api
sudo journalctl -u lumon-api -f
```

---

## 🔧 Переменные Окружения (.env)

**Файл:** `/home/user/lumon/back/api/.env`

```env
PORT=3000
NODE_ENV=production

# Database (Docker PostgreSQL)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=[СКРЫТО]
DB_DATABASE=lumon
DB_SSL=false

# OpenAI
OPENAI_API_KEY=sk-proj-[СКРЫТО]

# Telegram Bot
TELEGRAM_BOT_TOKEN=[СКРЫТО]

# Session
SESSION_EXPIRY_DAYS=7

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
```

---

## 📦 NPM Зависимости

### Основной проект (`/home/user/lumon/`)

**package.json:**
```json
{
  "name": "lumon2",
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "clsx": "^2.1.1",
    "framer-motion": "^12.23.24",
    "lucide-react": "^0.263.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.1",
    "tailwind-merge": "^3.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.2.15",
    "@types/react-dom": "^18.2.7",
    "@typescript-eslint/eslint-plugin": "^8.46.3",
    "@typescript-eslint/parser": "^8.46.3",
    "@vitejs/plugin-react": "^4.0.3",
    "autoprefixer": "^10.4.14",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "postcss": "^8.4.27",
    "prettier": "^3.6.2",
    "tailwindcss": "^3.3.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
```

**Статус:** ✅ `node_modules/` установлены (136 пакетов)

### Backend API (`/home/user/lumon/back/api/`)

**Статус:** ✅ `node_modules/` установлены (526 пакетов)
**TypeScript:** ✅ Скомпилирован в `dist/`

### Админка (`/home/user/lumon/adminpage/`)

**Статус:** ❌ `package.json` НЕ НАЙДЕН
**Статус:** ❌ `node_modules/` НЕ УСТАНОВЛЕНЫ

---

## 🔄 Git Статус

**Текущая ветка:** `claude/n8n-backend-discussion-01EyCeQ9q98KrPg4HanTuzyr`

**Последние коммиты:**
```
78e5d28 Merge pull request #63 from Psayha/claude/fix-cors-webhook-01GSBNv6DE9H3GjWiFjdDjj9
9ec69f7 feat: implement real database backup functionality
58735d5 Merge pull request #62
04a1ec1 feat: add backup management endpoints
42ba363 Merge pull request #61
```

**Статус:** ✅ Working tree clean (на 62 коммита впереди origin)

---

## ⚠️ Проблемы и Несоответствия

### 1. **Фронтенд рассинхронизирован**

- **В Git:** `/home/user/lumon/dist/` (17.11 20:23)
- **На веб-сервере:** `/var/www/lumon2/` (18.11 21:54) ← **ДРУГОЙ ИСТОЧНИК!**

**Последствия:** Фронтенд может использовать старый код, не соответствующий NestJS API

### 2. **Админка не собирается**

- `adminpage/package.json` не найден
- `adminpage/node_modules/` отсутствуют

**Последствия:** Невозможно собрать админку командой `npm run build`

### 3. **n8n контейнер все еще запущен**

- Docker контейнер `lumon-n8n` работает на порту 5678
- Но не используется (NestJS API заменил его функционал)

**Последствия:** Потребление ресурсов без необходимости

---

## ✅ Исправления

### Исправление #1: Синхронизация фронтенда

```bash
# Скопировать актуальную сборку на веб-сервер
cd /home/user/lumon
sudo cp -r dist/* /var/www/lumon2/
sudo cp -r dist-admin/* /var/www/lumon2/dist-admin/

# Проверить
ls -la /var/www/lumon2/index.html
```

### Исправление #2: Остановить n8n (необязательно)

```bash
# Остановить n8n контейнер
docker stop lumon-n8n

# Отключить автозапуск
docker update --restart=no lumon-n8n
```

---

## 📊 Текущий Статус Работы

| Компонент | Статус | Примечание |
|-----------|--------|------------|
| **NestJS API** | ✅ Работает | Порт 3000, все 31 endpoint работают |
| **PostgreSQL** | ✅ Работает | Docker, порт 5432 |
| **Nginx** | ✅ Работает | Проксирует на API |
| **Фронтенд** | ⚠️ Устаревший | Не синхронизирован с git |
| **Админка** | ⚠️ Работает | Но не собирается из исходников |
| **Чаты** | ✅ Работают | API сохраняет сообщения корректно |
| **История чатов** | ❌ Не отображается | Фронтенд устаревший |

---

## 🎯 Рекомендации

1. **Синхронизировать фронтенд** - скопировать dist/ на веб-сервер
2. **Восстановить adminpage/package.json** из git истории
3. **Остановить n8n контейнер** - он больше не нужен
4. **Создать процесс автоматического деплоя** через GitHub Actions
5. **Документировать процесс сборки** для adminpage

---

**Версия документа:** 1.0
**Автор:** Claude AI
**Дата:** 18 ноября 2025
