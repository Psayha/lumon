---
globs: front/**/*.tsx,packages/**/*.tsx
description: Основная архитектура проекта Lumon
---

# 🏗️ Project Architecture

## 📁 Структура проекта

Проект следует 3-слойной архитектуре:

```
lumon/
├── packages/              # 🎯 ОБЩИЕ ПАКЕТЫ (ЕДИНЫЙ СТАНДАРТ)
│   ├── ui/               # UI компоненты
│   ├── types/            # Типы данных
│   ├── api/              # API клиент
│   ├── analytics/        # Аналитика
│   └── utils/            # Утилиты
├── front/                # Frontend приложения
│   ├── tma/              # Telegram Mini App
│   └── admin-web/        # Admin Web приложение
├── back/                 # Backend сервисы
│   ├── n8n/              # n8n workflow automation
│   ├── api/              # REST API сервер
│   └── workers/          # Background workers
├── base/                 # Базы данных
│   ├── supabase/         # Supabase (PostgreSQL + Vector DB)
│   ├── postgres/         # Классическая PostgreSQL
│   └── redis/            # Redis для кэширования
```

## 🎯 КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА

### 1. **ВСЕ ОБЩИЕ КОМПОНЕНТЫ** в `packages/`
- ❌ НЕ создавать `front/shared/` папку
- ❌ НЕ делать прямые импорты между приложениями
- ✅ ВСЕ общие компоненты в `packages/`

### 2. **ПРАВИЛЬНЫЕ ИМПОРТЫ (ОБЯЗАТЕЛЬНО!):**
```typescript
// ✅ ПРАВИЛЬНО - всегда так
import { Card, Button } from '@lumon/ui';
import { User, Company } from '@lumon/types';
import { apiClient } from '@lumon/api';
import { trackEvent } from '@lumon/analytics';
import { formatDate } from '@lumon/utils';

// ❌ НЕПРАВИЛЬНО - больше никогда
import { Card } from './shared/components/Card';
import { User } from '../types/User';
import { Button } from '../../../packages/ui/src/Button';
```

### 3. **Стили только через Tailwind CSS**
- Использовать Tailwind CSS классы для всех стилей
- Context API для управления темами
- НЕ создавать отдельные CSS файлы

## 🔧 Технологический стек

- **Frontend**: React 18, TypeScript, Tailwind CSS
- **Backend**: n8n (workflow automation platform)
- **Build**: Vite 5, Turbo 2.0
- **State**: Zustand с persistence
- **Routing**: React Router v6
- **UI**: Lucide React, Custom Components
- **Forms**: React Hook Form + Zod валидация

## 📚 Документация

- [PROJECT_STRUCTURE.md](mdc:docs/PROJECT_STRUCTURE.md) - Структура проекта
- [FRONTEND_ARCHITECTURE.md](mdc:docs/FRONTEND_ARCHITECTURE.md) - Frontend архитектура
- [DEVELOPER_GUIDE.md](mdc:docs/DEVELOPER_GUIDE.md) - Руководство разработчика