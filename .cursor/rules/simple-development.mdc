---
alwaysApply: true
description: Правила простой разработки без усложнений
---

# 🎯 Simple Development Rules

## 🚫 ЗАПРЕЩЕНО УСЛОЖНЯТЬ

### ❌ НЕ создавать сложную архитектуру
- НЕ создавать папки `packages/`, `front/tma/`, `front/admin-web/`
- НЕ создавать monorepo структуру
- НЕ добавлять Turborepo, Vite, сложные build системы
- НЕ создавать сложные импорты `@lumon/*`

### ❌ НЕ усложнять компоненты
- НЕ разбивать простые компоненты на множество файлов
- НЕ создавать сложные хуки и утилиты
- НЕ добавлять избыточные TypeScript типы
- НЕ создавать сложные state management

### ❌ НЕ усложнять стили
- НЕ создавать CSS файлы
- НЕ добавлять сложные анимации
- НЕ создавать design system
- НЕ добавлять сложные Tailwind конфигурации

## ✅ РАЗРЕШЕНО - ТОЛЬКО ПРОСТОЕ

### ✅ Простая структура
```
lumon2/
├── front/                    # Только React компоненты
│   ├── MenuPage.tsx         # Простые страницы
│   └── VoiceAssistantPage.tsx
├── back/                     # Пусто или простые файлы
├── base/                     # Пусто или простые файлы
└── README.md
```

### ✅ Простые импорты
```typescript
// ✅ ПРАВИЛЬНО - только стандартные импорты
import React from 'react';
import { useNavigate } from 'react-router-dom';
import { Brain, Zap } from 'lucide-react';

// ❌ НЕПРАВИЛЬНО - сложные импорты
import { Card } from '@lumon/ui';
import { PageLayout } from '../../components/PageLayout';
```

### ✅ Простые компоненты
- Один файл = один компонент
- Минимум props и state
- Простые функции без сложной логики
- Только необходимые useEffect

### ✅ Простые стили
- Только Tailwind CSS классы
- Без inline стилей
- Без сложных анимаций
- Простые responsive классы

## 🎯 ПРИНЦИПЫ

1. **KISS** - Keep It Simple, Stupid
2. **YAGNI** - You Aren't Gonna Need It**
3. **Минимум зависимостей**
4. **Максимум читаемости**
5. **Без преждевременной оптимизации**

## 🚨 КРИТИЧЕСКИ ВАЖНО

- НЕ предлагать создание сложной архитектуры
- НЕ предлагать monorepo структуру
- НЕ предлагать сложные build системы
- НЕ предлагать сложные state management
- НЕ предлагать сложные TypeScript типы

## 📝 ПРИМЕРЫ ПРАВИЛЬНОГО ПОДХОДА

### ✅ Простой компонент
```tsx
const SimpleComponent = () => {
  const [count, setCount] = useState(0);
  
  return (
    <div className="p-4 bg-white rounded-lg">
      <h1 className="text-xl font-bold">Simple</h1>
      <p>Count: {count}</p>
      <button 
        onClick={() => setCount(count + 1)}
        className="px-4 py-2 bg-blue-500 text-white rounded"
      >
        Increment
      </button>
    </div>
  );
};
```

### ❌ Сложный компонент (НЕ ДЕЛАТЬ)
```tsx
// Сложные типы, хуки, утилиты - НЕ НУЖНО
const ComplexComponent = () => {
  // Сложная логика - НЕ НУЖНО
};
```

---

**ПОМНИ**: Простота - это красота. Не усложняй то, что работает.