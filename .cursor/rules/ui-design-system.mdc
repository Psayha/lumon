---
globs: front/**/*.tsx,packages/**/*.tsx
description: UI Design System
---

# 🎨 UI Design System

## 🎯 Основные принципы Design System

### ✅ TAILWIND CSS ПОДХОД
- Используйте utility-first подход
- Применяйте консистентные spacing и colors
- Создавайте переиспользуемые компоненты
- Используйте semantic color names

### ✅ СЕМАНТИЧЕСКИЕ ЦВЕТА
- Primary - основные действия
- Secondary - второстепенные действия
- Success - успешные операции
- Warning - предупреждения
- Error - ошибки и опасные действия
- Info - информационные сообщения

### ✅ КОМПОНЕНТЫ
- Button - кнопки с вариантами
- Card - карточки для контента
- Input - поля ввода
- Modal - модальные окна
- Navigation - навигация

## 📝 ПРИМЕРЫ КОМПОНЕНТОВ

### ✅ Button компонент
```tsx
import React from 'react';

interface ButtonProps {
  children: React.ReactNode;
  onClick?: () => void;
  variant?: 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'info';
  size?: 'sm' | 'md' | 'lg';
  disabled?: boolean;
  loading?: boolean;
  className?: string;
}

export const Button: React.FC<ButtonProps> = ({
  children,
  onClick,
  variant = 'primary',
  size = 'md',
  disabled = false,
  loading = false,
  className = ''
}) => {
  const getVariantClasses = () => {
    switch (variant) {
      case 'primary':
        return 'bg-blue-600 hover:bg-blue-700 text-white focus:ring-blue-500';
      case 'secondary':
        return 'bg-gray-600 hover:bg-gray-700 text-white focus:ring-gray-500';
      case 'success':
        return 'bg-green-600 hover:bg-green-700 text-white focus:ring-green-500';
      case 'warning':
        return 'bg-yellow-600 hover:bg-yellow-700 text-white focus:ring-yellow-500';
      case 'error':
        return 'bg-red-600 hover:bg-red-700 text-white focus:ring-red-500';
      case 'info':
        return 'bg-indigo-600 hover:bg-indigo-700 text-white focus:ring-indigo-500';
      default:
        return 'bg-blue-600 hover:bg-blue-700 text-white focus:ring-blue-500';
    }
  };

  const getSizeClasses = () => {
    switch (size) {
      case 'sm':
        return 'px-3 py-1.5 text-sm';
      case 'md':
        return 'px-4 py-2 text-base';
      case 'lg':
        return 'px-6 py-3 text-lg';
      default:
        return 'px-4 py-2 text-base';
    }
  };

  return (
    <button
      onClick={onClick}
      disabled={disabled || loading}
      className={`
        inline-flex items-center justify-center
        font-medium rounded-lg
        transition-colors duration-150
        focus:outline-none focus:ring-2 focus:ring-offset-2
        disabled:opacity-50 disabled:cursor-not-allowed
        ${getVariantClasses()}
        ${getSizeClasses()}
        ${className}
      `}
    >
      {loading ? (
        <div className="flex items-center">
          <div className="animate-spin rounded-full h-4 w-4 border-b-2 border-current mr-2"></div>
          Загрузка...
        </div>
      ) : (
        children
      )}
    </button>
  );
};
```

### ✅ Card компонент
```tsx
import React from 'react';

interface CardProps {
  children: React.ReactNode;
  className?: string;
  variant?: 'default' | 'elevated' | 'outlined';
  padding?: 'sm' | 'md' | 'lg';
}

export const Card: React.FC<CardProps> = ({
  children,
  className = '',
  variant = 'default',
  padding = 'md'
}) => {
  const getVariantClasses = () => {
    switch (variant) {
      case 'elevated':
        return 'shadow-lg';
      case 'outlined':
        return 'border-2 border-gray-200';
      default:
        return 'shadow-md';
    }
  };

  const getPaddingClasses = () => {
    switch (padding) {
      case 'sm':
        return 'p-3';
      case 'md':
        return 'p-4';
      case 'lg':
        return 'p-6';
      default:
        return 'p-4';
    }
  };

  return (
    <div
      className={`
        bg-white dark:bg-gray-800
        rounded-lg
        border border-gray-200 dark:border-gray-700
        ${getVariantClasses()}
        ${getPaddingClasses()}
        ${className}
      `}
    >
      {children}
    </div>
  );
};
```

### ✅ Input компонент
```tsx
import React from 'react';

interface InputProps {
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  type?: 'text' | 'email' | 'password' | 'number';
  disabled?: boolean;
  error?: string;
  label?: string;
  required?: boolean;
  className?: string;
}

export const Input: React.FC<InputProps> = ({
  value,
  onChange,
  placeholder = '',
  type = 'text',
  disabled = false,
  error,
  label,
  required = false,
  className = ''
}) => {
  return (
    <div className={`space-y-2 ${className}`}>
      {label && (
        <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
          {label}
          {required && <span className="text-red-500 ml-1">*</span>}
        </label>
      )}
      
      <input
        type={type}
        value={value}
        onChange={(e) => onChange(e.target.value)}
        placeholder={placeholder}
        disabled={disabled}
        required={required}
        className={`
          w-full px-3 py-2
          border rounded-lg
          bg-white dark:bg-gray-700
          text-gray-900 dark:text-white
          placeholder-gray-500 dark:placeholder-gray-400
          focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent
          disabled:opacity-50 disabled:cursor-not-allowed
          transition-colors duration-150
          ${error 
            ? 'border-red-500 focus:ring-red-500' 
            : 'border-gray-300 dark:border-gray-600'
          }
        `}
      />
      
      {error && (
        <p className="text-sm text-red-600 dark:text-red-400">
          {error}
        </p>
      )}
    </div>
  );
};
```

### ✅ Modal компонент
```tsx
import React from 'react';
import { X } from 'lucide-react';

interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
  size?: 'sm' | 'md' | 'lg' | 'xl';
  showCloseButton?: boolean;
}

export const Modal: React.FC<ModalProps> = ({
  isOpen,
  onClose,
  title,
  children,
  size = 'md',
  showCloseButton = true
}) => {
  const getSizeClasses = () => {
    switch (size) {
      case 'sm':
        return 'max-w-md';
      case 'md':
        return 'max-w-lg';
      case 'lg':
        return 'max-w-2xl';
      case 'xl':
        return 'max-w-4xl';
      default:
        return 'max-w-lg';
    }
  };

  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 z-50 overflow-y-auto">
      <div className="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
        {/* Backdrop */}
        <div
          className="fixed inset-0 transition-opacity bg-gray-500 bg-opacity-75"
          onClick={onClose}
        />
        
        {/* Modal */}
        <div
          className={`
            inline-block w-full
            ${getSizeClasses()}
            p-6 my-8
            text-left align-middle
            bg-white dark:bg-gray-800
            rounded-lg shadow-xl
            transform transition-all
          `}
        >
          {/* Header */}
          {(title || showCloseButton) && (
            <div className="flex items-center justify-between mb-4">
              {title && (
                <h3 className="text-lg font-medium text-gray-900 dark:text-white">
                  {title}
                </h3>
              )}
              
              {showCloseButton && (
                <button
                  onClick={onClose}
                  className="p-1 text-gray-400 hover:text-gray-600 dark:hover:text-gray-300 transition-colors"
                >
                  <X className="w-5 h-5" />
                </button>
              )}
            </div>
          )}
          
          {/* Content */}
          <div className="text-gray-700 dark:text-gray-300">
            {children}
          </div>
        </div>
      </div>
    </div>
  );
};
```

### ✅ Navigation компонент
```tsx
import React from 'react';
import { Link, useLocation } from 'react-router-dom';

interface NavigationItem {
  name: string;
  href: string;
  icon?: React.ComponentType<{ className?: string }>;
}

interface NavigationProps {
  items: NavigationItem[];
  className?: string;
}

export const Navigation: React.FC<NavigationProps> = ({
  items,
  className = ''
}) => {
  const location = useLocation();

  return (
    <nav className={`bg-white dark:bg-gray-800 shadow-sm ${className}`}>
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between h-16">
          <div className="flex">
            <div className="flex-shrink-0 flex items-center">
              <Link to="/" className="text-xl font-bold text-gray-900 dark:text-white">
                Lumon
              </Link>
            </div>
            
            <div className="hidden sm:ml-6 sm:flex sm:space-x-8">
              {items.map((item) => {
                const isActive = location.pathname === item.href;
                
                return (
                  <Link
                    key={item.name}
                    to={item.href}
                    className={`
                      inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium
                      transition-colors duration-150
                      ${isActive
                        ? 'border-blue-500 text-gray-900 dark:text-white'
                        : 'border-transparent text-gray-500 hover:text-gray-700 dark:hover:text-gray-300 hover:border-gray-300'
                      }
                    `}
                  >
                    {item.icon && <item.icon className="w-4 h-4 mr-2" />}
                    {item.name}
                  </Link>
                );
              })}
            </div>
          </div>
        </div>
      </div>
    </nav>
  );
};
```

## 🎨 ЦВЕТОВАЯ ПАЛИТРА

### Основные цвета
```tsx
// ✅ ПРАВИЛЬНО - semantic colors
const colors = {
  primary: {
    50: 'bg-blue-50',
    100: 'bg-blue-100',
    500: 'bg-blue-500',
    600: 'bg-blue-600',
    700: 'bg-blue-700',
    900: 'bg-blue-900'
  },
  secondary: {
    50: 'bg-gray-50',
    100: 'bg-gray-100',
    500: 'bg-gray-500',
    600: 'bg-gray-600',
    700: 'bg-gray-700',
    900: 'bg-gray-900'
  },
  success: {
    50: 'bg-green-50',
    100: 'bg-green-100',
    500: 'bg-green-500',
    600: 'bg-green-600',
    700: 'bg-green-700',
    900: 'bg-green-900'
  },
  warning: {
    50: 'bg-yellow-50',
    100: 'bg-yellow-100',
    500: 'bg-yellow-500',
    600: 'bg-yellow-600',
    700: 'bg-yellow-700',
    900: 'bg-yellow-900'
  },
  error: {
    50: 'bg-red-50',
    100: 'bg-red-100',
    500: 'bg-red-500',
    600: 'bg-red-600',
    700: 'bg-red-700',
    900: 'bg-red-900'
  }
};
```

## 🎯 ЛУЧШИЕ ПРАКТИКИ

### 1. **Используйте консистентные spacing**
```tsx
// ✅ ПРАВИЛЬНО - Tailwind spacing scale
<div className="p-4 m-2 space-y-4">
```

### 2. **Применяйте semantic цвета**
```tsx
// ✅ ПРАВИЛЬНО - semantic color names
<button className="bg-primary-600 hover:bg-primary-700">
```

### 3. **Создавайте переиспользуемые компоненты**
```tsx
// ✅ ПРАВИЛЬНО - компонентный подход
<Button variant="primary" size="lg">Submit</Button>
```

### 4. **Поддерживайте темную тему**
```tsx
// ✅ ПРАВИЛЬНО - dark mode support
<div className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
```

## 🚨 КРИТИЧЕСКИ ВАЖНО

### ❌ НЕ ДЕЛАЙТЕ
```tsx
// ❌ НЕПРАВИЛЬНО - inline стили
<div style={{ backgroundColor: 'blue', padding: '16px' }}>

// ❌ НЕПРАВИЛЬНО - CSS файлы
import './Button.css';

// ❌ НЕПРАВИЛЬНО - сложные design tokens
const designTokens = {
  colors: {
    primary: {
      light: { value: '#3b82f6', type: 'color' },
      main: { value: '#1d4ed8', type: 'color' },
      dark: { value: '#1e40af', type: 'color' }
    }
  }
};

// ❌ НЕПРАВИЛЬНО - игнорирование accessibility
<button className="bg-blue-500 text-white">Click</button>
```

### ✅ ДЕЛАЙТЕ ТАК
```tsx
// ✅ ПРАВИЛЬНО - Tailwind классы
<div className="bg-blue-500 p-4">

// ✅ ПРАВИЛЬНО - компонентный подход
<Button variant="primary">Click</Button>

// ✅ ПРАВИЛЬНО - простые цвета
const colors = {
  primary: 'bg-blue-500',
  secondary: 'bg-gray-500'
};

// ✅ ПРАВИЛЬНО - accessibility
<button 
  className="bg-blue-500 text-white focus:ring-2 focus:ring-blue-500"
  aria-label="Submit form"
>
  Submit
</button>
```

---

**Помни**: Design System должен быть простым и консистентным. Используйте Tailwind CSS и создавайте переиспользуемые компоненты.