---
globs: front/**/*.tsx,packages/**/*.tsx
description: Тестирование Telegram функций
---

# 🧪 Telegram Testing

## 🎯 Принципы тестирования Telegram Mini Apps

### ✅ СТРАТЕГИЯ ТЕСТИРОВАНИЯ
- Unit тесты для компонентов
- Integration тесты для Telegram API
- E2E тесты для пользовательских сценариев
- Mock объекты для Telegram WebApp

### ✅ ТЕСТИРОВАНИЕ КОМПОНЕНТОВ
- Тестирование адаптации к темам
- Проверка haptic feedback
- Тестирование навигации
- Проверка валидации форм

### ✅ ТЕСТИРОВАНИЕ API
- Тестирование Telegram WebApp API
- Проверка обработки ошибок
- Тестирование аутентификации
- Проверка интеграции с Bot API

## 📝 ПРИМЕРЫ ТЕСТИРОВАНИЯ

### ✅ Unit тесты для Telegram компонентов
```tsx
// TelegramButton.test.tsx
import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';
import { TelegramButton } from '../TelegramButton';

// Mock для useTelegramTheme
jest.mock('../hooks/useTelegramTheme', () => ({
  useTelegramTheme: () => ({
    themeParams: {
      button_color: '#2481cc',
      button_text_color: '#ffffff'
    }
  })
}));

describe('TelegramButton', () => {
  it('renders with correct text', () => {
    render(<TelegramButton>Click me</TelegramButton>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const handleClick = jest.fn();
    render(<TelegramButton onClick={handleClick}>Click me</TelegramButton>);
    
    fireEvent.click(screen.getByText('Click me'));
    expect(handleClick).toHaveBeenCalledTimes(1);
  });

  it('shows loading state', () => {
    render(<TelegramButton loading={true}>Click me</TelegramButton>);
    expect(screen.getByRole('button')).toBeDisabled();
  });

  it('applies correct styles based on variant', () => {
    const { rerender } = render(<TelegramButton variant="primary">Primary</TelegramButton>);
    expect(screen.getByRole('button')).toHaveStyle({
      backgroundColor: '#2481cc',
      color: '#ffffff'
    });

    rerender(<TelegramButton variant="secondary">Secondary</TelegramButton>);
    expect(screen.getByRole('button')).toHaveStyle({
      backgroundColor: '#f1f1f1',
      color: '#000000'
    });
  });
});
```

### ✅ Тестирование Telegram хуков
```tsx
// useTelegram.test.ts
import { renderHook } from '@testing-library/react';
import { useTelegram } from '../useTelegram';

// Mock для window.Telegram
const mockTelegramWebApp = {
  ready: jest.fn(),
  close: jest.fn(),
  BackButton: {
    show: jest.fn(),
    hide: jest.fn(),
    onClick: jest.fn()
  },
  MainButton: {
    show: jest.fn(),
    hide: jest.fn(),
    setText: jest.fn(),
    onClick: jest.fn()
  },
  HapticFeedback: {
    impactOccurred: jest.fn()
  }
};

describe('useTelegram', () => {
  beforeEach(() => {
    // Очищаем моки
    jest.clearAllMocks();
    
    // Устанавливаем mock для window.Telegram
    (window as any).Telegram = {
      WebApp: mockTelegramWebApp
    };
  });

  afterEach(() => {
    // Очищаем window.Telegram
    delete (window as any).Telegram;
  });

  it('returns telegram instance when available', () => {
    const { result } = renderHook(() => useTelegram());
    
    expect(result.current.tg).toBe(mockTelegramWebApp);
    expect(result.current.isReady).toBe(true);
  });

  it('returns null when telegram is not available', () => {
    delete (window as any).Telegram;
    
    const { result } = renderHook(() => useTelegram());
    
    expect(result.current.tg).toBeNull();
    expect(result.current.isReady).toBe(false);
  });

  it('calls ready() when telegram is available', () => {
    renderHook(() => useTelegram());
    
    expect(mockTelegramWebApp.ready).toHaveBeenCalledTimes(1);
  });
});
```

### ✅ Integration тесты для Telegram API
```tsx
// TelegramAPI.test.ts
import { TelegramAPI } from '../TelegramAPI';

// Mock для fetch
global.fetch = jest.fn();

describe('TelegramAPI', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('sendMessage', () => {
    it('sends message successfully', async () => {
      const mockResponse = { ok: true, json: () => Promise.resolve({ result: { message_id: 123 } }) };
      (fetch as jest.Mock).mockResolvedValue(mockResponse);

      const result = await TelegramAPI.sendMessage('123456', 'Hello World');

      expect(fetch).toHaveBeenCalledWith(
        'https://api.telegram.org/bot<TOKEN>/sendMessage',
        expect.objectContaining({
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            chat_id: '123456',
            text: 'Hello World'
          })
        })
      );
      expect(result).toEqual({ message_id: 123 });
    });

    it('handles API errors', async () => {
      const mockResponse = { ok: false, status: 400, json: () => Promise.resolve({ error_code: 400, description: 'Bad Request' }) };
      (fetch as jest.Mock).mockResolvedValue(mockResponse);

      await expect(TelegramAPI.sendMessage('123456', 'Hello World')).rejects.toThrow('Bad Request');
    });
  });

  describe('getUserInfo', () => {
    it('returns user information', async () => {
      const mockUser = { id: 123, first_name: 'John', username: 'john_doe' };
      const mockResponse = { ok: true, json: () => Promise.resolve({ result: mockUser }) };
      (fetch as jest.Mock).mockResolvedValue(mockResponse);

      const result = await TelegramAPI.getUserInfo('123');

      expect(result).toEqual(mockUser);
    });
  });
});
```

### ✅ E2E тесты для Telegram Mini App
```tsx
// TMA.e2e.test.ts
import { test, expect } from '@playwright/test';

test.describe('Telegram Mini App', () => {
  test.beforeEach(async ({ page }) => {
    // Mock Telegram WebApp
    await page.addInitScript(() => {
      window.Telegram = {
        WebApp: {
          ready: () => {},
          close: () => {},
          initData: 'user=%7B%22id%22%3A123%2C%22first_name%22%3A%22John%22%7D',
          initDataUnsafe: {
            user: { id: 123, first_name: 'John' }
          },
          themeParams: {
            bg_color: '#ffffff',
            text_color: '#000000',
            button_color: '#2481cc'
          },
          BackButton: {
            show: () => {},
            hide: () => {},
            onClick: () => {}
          },
          MainButton: {
            show: () => {},
            hide: () => {},
            setText: () => {},
            onClick: () => {}
          },
          HapticFeedback: {
            impactOccurred: () => {}
          }
        }
      };
    });

    await page.goto('/');
  });

  test('displays welcome message with user name', async ({ page }) => {
    await expect(page.getByText('Добро пожаловать, John!')).toBeVisible();
  });

  test('navigates to voice assistant page', async ({ page }) => {
    await page.click('text=🎤 Голосовой ассистент');
    await expect(page.getByText('Голосовой ассистент')).toBeVisible();
  });

  test('shows main button when required', async ({ page }) => {
    await page.click('text=⚙️ Настройки');
    await expect(page.getByRole('button', { name: 'Сохранить' })).toBeVisible();
  });

  test('handles form submission', async ({ page }) => {
    await page.click('text=Обратная связь');
    
    await page.fill('input[placeholder="Введите ваше имя"]', 'John Doe');
    await page.fill('input[placeholder="your@email.com"]', 'john@example.com');
    await page.fill('textarea[placeholder*="Опишите ваш вопрос"]', 'Test message');
    
    await page.click('button:has-text("Отправить")');
    
    await expect(page.getByText('Сообщение отправлено')).toBeVisible();
  });
});
```

### ✅ Mock объекты для Telegram
```tsx
// __mocks__/telegram.ts
export const mockTelegramWebApp = {
  ready: jest.fn(),
  close: jest.fn(),
  expand: jest.fn(),
  initData: 'user=%7B%22id%22%3A123%2C%22first_name%22%3A%22John%22%7D',
  initDataUnsafe: {
    user: {
      id: 123,
      first_name: 'John',
      last_name: 'Doe',
      username: 'john_doe',
      language_code: 'en'
    }
  },
  themeParams: {
    bg_color: '#ffffff',
    text_color: '#000000',
    hint_color: '#999999',
    link_color: '#2481cc',
    button_color: '#2481cc',
    button_text_color: '#ffffff',
    secondary_bg_color: '#f8f9fa'
  },
  colorScheme: 'light',
  BackButton: {
    show: jest.fn(),
    hide: jest.fn(),
    onClick: jest.fn()
  },
  MainButton: {
    show: jest.fn(),
    hide: jest.fn(),
    setText: jest.fn(),
    onClick: jest.fn(),
    showProgress: jest.fn(),
    hideProgress: jest.fn()
  },
  HapticFeedback: {
    impactOccurred: jest.fn(),
    notificationOccurred: jest.fn(),
    selectionChanged: jest.fn()
  },
  setHeaderColor: jest.fn(),
  setBackgroundColor: jest.fn()
};

// Устанавливаем mock в глобальном scope
Object.defineProperty(window, 'Telegram', {
  value: { WebApp: mockTelegramWebApp },
  writable: true
});
```

### ✅ Тестирование адаптации к темам
```tsx
// ThemeAdaptation.test.tsx
import React from 'react';
import { render, screen } from '@testing-library/react';
import { TelegramCard } from '../TelegramCard';

// Mock для useTelegramTheme
const mockUseTelegramTheme = jest.fn();

jest.mock('../hooks/useTelegramTheme', () => ({
  useTelegramTheme: () => mockUseTelegramTheme()
}));

describe('Theme Adaptation', () => {
  it('adapts to light theme', () => {
    mockUseTelegramTheme.mockReturnValue({
      themeParams: {
        bg_color: '#ffffff',
        text_color: '#000000',
        secondary_bg_color: '#f8f9fa'
      }
    });

    render(<TelegramCard>Test content</TelegramCard>);
    
    const card = screen.getByText('Test content').parentElement;
    expect(card).toHaveStyle({
      backgroundColor: '#f8f9fa'
    });
  });

  it('adapts to dark theme', () => {
    mockUseTelegramTheme.mockReturnValue({
      themeParams: {
        bg_color: '#1a1a1a',
        text_color: '#ffffff',
        secondary_bg_color: '#2a2a2a'
      }
    });

    render(<TelegramCard>Test content</TelegramCard>);
    
    const card = screen.getByText('Test content').parentElement;
    expect(card).toHaveStyle({
      backgroundColor: '#2a2a2a'
    });
  });
});
```

## 🎯 ЛУЧШИЕ ПРАКТИКИ

### 1. **Используйте простые mock объекты**
```tsx
// ✅ ПРАВИЛЬНО - простые моки
const mockTelegram = {
  WebApp: {
    ready: jest.fn(),
    close: jest.fn()
  }
};
```

### 2. **Тестируйте пользовательские сценарии**
```tsx
// ✅ ПРАВИЛЬНО - тестирование сценариев
test('user can complete onboarding flow', async ({ page }) => {
  await page.click('text=Начать');
  await page.fill('input[name="name"]', 'John');
  await page.click('text=Продолжить');
  await expect(page.getByText('Добро пожаловать, John!')).toBeVisible();
});
```

### 3. **Проверяйте обработку ошибок**
```tsx
// ✅ ПРАВИЛЬНО - тестирование ошибок
it('handles API errors gracefully', async () => {
  const mockError = new Error('API Error');
  jest.spyOn(TelegramAPI, 'sendMessage').mockRejectedValue(mockError);
  
  const { result } = renderHook(() => useTelegramAPI());
  
  await act(async () => {
    await result.current.sendMessage('Hello');
  });
  
  expect(result.current.error).toBe(mockError);
});
```

## 🚨 КРИТИЧЕСКИ ВАЖНО

### ❌ НЕ ДЕЛАЙТЕ
```tsx
// ❌ НЕПРАВИЛЬНО - сложные mock объекты
const complexMock = {
  WebApp: {
    // Слишком много методов
  }
};

// ❌ НЕПРАВИЛЬНО - игнорирование тестирования
const BadComponent = () => {
  // Нет тестов
};

// ❌ НЕПРАВИЛЬНО - тестирование реализации
it('calls internal method', () => {
  // Тестирование внутренней логики
});
```

### ✅ ДЕЛАЙТЕ ТАК
```tsx
// ✅ ПРАВИЛЬНО - простые моки
const simpleMock = {
  WebApp: { ready: jest.fn() }
};

// ✅ ПРАВИЛЬНО - тестирование поведения
it('shows loading state', () => {
  render(<Component loading={true} />);
  expect(screen.getByText('Loading...')).toBeVisible();
});

// ✅ ПРАВИЛЬНО - тестирование пользовательского опыта
it('allows user to submit form', async () => {
  render(<Form />);
  await user.type(screen.getByLabelText('Name'), 'John');
  await user.click(screen.getByRole('button', { name: 'Submit' }));
  expect(screen.getByText('Success!')).toBeVisible();
});
```

---

**Помни**: Тестирование TMA должно быть простым и фокусироваться на пользовательском опыте.

## 🆕 Дополнения к тестам (Bot API 7.0 → 9.1)

### Расширенный mock Telegram WebApp
```tsx
// __mocks__/telegram-extended.ts
export const mockTelegramWebApp = {
  ready: jest.fn(),
  close: jest.fn(),
  expand: jest.fn(),
  isFullscreen: false,
  requestFullscreen: jest.fn(function() { (this as any).isFullscreen = true; }),
  exitFullscreen: jest.fn(function() { (this as any).isFullscreen = false; }),
  onEvent: jest.fn(),
  addToHomeScreen: jest.fn(),
  checkHomeScreenStatus: jest.fn(),
  shareMessage: jest.fn(),
  downloadFile: jest.fn(),
  readTextFromClipboard: jest.fn().mockResolvedValue('clipboard text'),
  showScanQrPopup: jest.fn(),
  closeScanQrPopup: jest.fn(),
  requestWriteAccess: jest.fn(),
  requestContact: jest.fn(),
  hideKeyboard: jest.fn(),
  setHeaderColor: jest.fn(),
  setBackgroundColor: jest.fn(),
  enableVerticalSwipes: jest.fn(),
  disableVerticalSwipes: jest.fn(),
  LocationManager: { requestLocation: jest.fn() },
  CloudStorage: { setItem: jest.fn(), getItem: jest.fn(), removeItem: jest.fn() },
  DeviceStorage: { setItem: jest.fn(), getItem: jest.fn(), removeItem: jest.fn() },
  SecureStorage: { setItem: jest.fn(), getItem: jest.fn(), removeItem: jest.fn() },
  BottomButton: { show: jest.fn(), hide: jest.fn(), setText: jest.fn(), onClick: jest.fn() },
  SecondaryButton: { show: jest.fn(), hide: jest.fn() },
  SettingsButton: { show: jest.fn(), hide: jest.fn() },
  BackButton: { show: jest.fn(), hide: jest.fn(), onClick: jest.fn() },
  MainButton: { show: jest.fn(), hide: jest.fn(), setText: jest.fn(), onClick: jest.fn(), showProgress: jest.fn(), hideProgress: jest.fn() },
  HapticFeedback: { impactOccurred: jest.fn(), notificationOccurred: jest.fn(), selectionChanged: jest.fn() },
  initData: '',
  initDataUnsafe: {
    user: { id: 1, first_name: 'Test' },
    theme_params: { bg_color: '#fff', text_color: '#000', button_color: '#2481cc' }
  },
  themeParams: { bg_color: '#fff', text_color: '#000', button_color: '#2481cc' },
  colorScheme: 'light'
};

Object.defineProperty(window, 'Telegram', { value: { WebApp: mockTelegramWebApp }, writable: true });
```

### Тест: Bottom/Secondary/Settings Buttons
```tsx
import { renderHook, act } from '@testing-library/react';
import { useTelegram } from '../useTelegram';

jest.mock('../__mocks__/telegram-extended');

it('shows bottom button and handles click', () => {
  const { result } = renderHook(() => useTelegram());
  const tg = result.current.tg as any;
  act(() => {
    tg.BottomButton.show();
    tg.BottomButton.setText('Go');
    tg.BottomButton.onClick.mock.calls[0]?.[0]?.();
  });
  expect(tg.BottomButton.show).toHaveBeenCalled();
  expect(tg.BottomButton.setText).toHaveBeenCalledWith('Go');
});
```

### Тест: Полноэкранный режим и safe area события
```tsx
it('toggles fullscreen', () => {
  const tg = (window as any).Telegram.WebApp;
  expect(tg.isFullscreen).toBe(false);
  tg.requestFullscreen();
  expect(tg.isFullscreen).toBe(true);
  tg.exitFullscreen();
  expect(tg.isFullscreen).toBe(false);
});
```

### Тест: Add to Home, Sharing, Download
```tsx
it('adds to home screen', async () => {
  const tg = (window as any).Telegram.WebApp;
  await tg.addToHomeScreen();
  expect(tg.addToHomeScreen).toHaveBeenCalled();
});

it('shares message and downloads file', async () => {
  const tg = (window as any).Telegram.WebApp;
  await tg.shareMessage({ text: 'Hi' });
  await tg.downloadFile({ url: 'https://example.com/file.pdf' });
  expect(tg.shareMessage).toHaveBeenCalled();
  expect(tg.downloadFile).toHaveBeenCalled();
});
```

### Тест: Геолокация и хранилища
```tsx
it('requests location', async () => {
  const tg = (window as any).Telegram.WebApp;
  await tg.LocationManager.requestLocation();
  expect(tg.LocationManager.requestLocation).toHaveBeenCalled();
});

it('uses storages', async () => {
  const tg = (window as any).Telegram.WebApp;
  await tg.CloudStorage.setItem('k', 'v');
  await tg.DeviceStorage.setItem('k', 'v');
  await tg.SecureStorage.setItem('k', 'secret');
  expect(tg.CloudStorage.setItem).toHaveBeenCalledWith('k', 'v');
  expect(tg.DeviceStorage.setItem).toHaveBeenCalledWith('k', 'v');
  expect(tg.SecureStorage.setItem).toHaveBeenCalledWith('k', 'secret');
});
```

### Playwright: e2e для новых UX
```tsx
test('can toggle fullscreen and share', async ({ page }) => {
  await page.addInitScript(() => {
    // Упростим: только нужные методы
    window.Telegram = { WebApp: {
      requestFullscreen: () => {}, exitFullscreen: () => {},
      shareMessage: () => {}, addToHomeScreen: () => {},
    }} as any;
  });
  await page.goto('/');
  await expect(page).toHaveTitle(/.+/);
});
```

### Отладка: включение WebView инспекции
- iOS: Settings → 10x tap → Allow Web View Inspection → Safari Develop.
- Android: Settings → удержать версию → Enable WebView Debug → chrome://inspect.
- Desktop beta: включить webview inspection, Inspect Element.